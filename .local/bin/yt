#!/usr/bin/env bash

source /usr/local/bin/userenv export_env BASH_LIB
source /usr/local/bin/userenv source_fzfrc

_yt() {
  exec "${BASH_LIB}/utils/ytx"
}

if [[ "${#BASH_SOURCE[@]}" -eq 1 ]]; then
  _yt
fi
